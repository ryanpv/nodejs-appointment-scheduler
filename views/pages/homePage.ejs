<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head.ejs') %>
</head>
<body class="container">
  <header>
    <%- include('../partials/header.ejs') %>
  </header>

  <div class="card rounded-1"  style="background-color: rgb(155, 164, 174);">
    <h1>Example Service</h1>
    <p>Schedule an appointment using this site. Create an account to see all booked appointments or to update/cancel appointments.</p>
    <hr class="my-4">
    <p>Start by <a href="/booking-form">booking now</a> or <a href="/signup-page">create an account</a>.</p>
  </div>
  
</body>
<script>
  const cookies = document.cookie
  console.log('cookies ejs: ', cookies.split("; "));
  if (cookies.split("; ").includes("currentUser=null")) {
    console.log('no user ejs: ',cookies.split("; ").includes("currentUser=null"));
  } else {
    console.log('current user ejs: ', cookies);
  }
  console.log('query selector: ', document.querySelectorAll('.content.unauthorized'));
  </script>

</html>